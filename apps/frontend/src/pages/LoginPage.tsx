import { useState } from "react"; import { useNavigate } from "react-router-dom"; import { login as loginApi, register, registerClient } from "../api/auth"; import { useAuthStore } from "../store/authStore";
export default function LoginPage() { const [mode, setMode] = useState<"login" | "customer" | "partner">("login"); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [phone, setPhone] = useState(""); const [businessName, setBusinessName] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const navigate = useNavigate(); const login = useAuthStore(s => s.login);
 const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); setError(""); setLoading(true); try { const result = mode === "login" ? await loginApi(email, password) : mode === "partner" ? await registerClient({ email, password, name, phone, businessName }) : await register({ email, password, name }); login(result.user, result.token); navigate(result.user.role === "SYSTEM_ADMIN" ? "/admin" : result.user.role === "CLIENT" ? "/partner" : "/movies"); } catch (err: any) { setError(err.response?.data?.message ?? "We couldn’t complete that request. Please try again."); setLoading(false); } };
 const title = mode === "login" ? "Your next story awaits." : mode === "partner" ? "Bring your theatre online." : "Start with a great story.";
 return <main className="auth-page"><section className="auth-card"><div className="auth-intro"><div className="brand"><span className="brand-mark">R</span>reservit</div><p>Film nights deserve a little ceremony. Find your seat, skip the queue, and settle in.</p><div className="auth-quote">“A ticket to somewhere else, for a little while.”</div></div><form className="auth-form" onSubmit={handleSubmit}><span className="eyebrow">{mode === "login" ? "Welcome back" : "Join Reservit"}</span><h1>{title}</h1><p>{mode === "partner" ? "Register your cinema. An administrator will review your partner account." : "Discover films and reserve your perfect seat."}</p><div className="auth-tabs"><button type="button" onClick={() => setMode("login")} className={mode === "login" ? "active" : ""}>Sign in</button><button type="button" onClick={() => setMode("customer")} className={mode === "customer" ? "active" : ""}>Create account</button><button type="button" onClick={() => setMode("partner")} className={mode === "partner" ? "active" : ""}>Theatre partner</button></div>{mode === "login" && <p className="admin-note">System administrator? Use your admin email and password here; you’ll be taken directly to the control room.</p>}{error && <div className="error">{error}</div>}{mode !== "login" && <label className="field">YOUR NAME<input value={name} onChange={e => setName(e.target.value)} required /></label>}{mode === "partner" && <><label className="field">CINEMA / BUSINESS NAME<input value={businessName} onChange={e => setBusinessName(e.target.value)} required /></label><label className="field">PHONE NUMBER<input value={phone} onChange={e => setPhone(e.target.value)} required /></label></>}<label className="field">EMAIL ADDRESS<input type="email" placeholder="you@example.com" value={email} onChange={e => setEmail(e.target.value)} required /></label><label className="field">PASSWORD<input type="password" placeholder={mode === "login" ? "Enter your password" : "At least 12 characters"} value={password} onChange={e => setPassword(e.target.value)} required minLength={mode === "login" ? 1 : 12} /></label><button className="btn" type="submit" disabled={loading}>{loading ? "Working…" : mode === "login" ? "Continue to Reservit →" : "Create my account →"}</button></form></section></main>; }
